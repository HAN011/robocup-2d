给 B 的说明（规则策略算法工程师）
项目背景
这是一个 RoboCup 2D 仿真足球项目，采用 规则策略 + 强化学习混合架构：
- 10号球员由 RL 模型控制
- 其余球员（包括守门员）由规则策略控制
- 规则策略的好坏直接影响 RL 训练质量（队友越强，RL 越容易学好）
你负责的文件
1. player/decision.py — 规则策略核心
当前 18 个动作 ID（不要改这个列表，A 的 RL 依赖它）：
0  = shoot
1  = dribble_forward
2  = dribble_left
3  = dribble_right
4-14 = pass to teammate unum 1~11
15 = chase_ball
16 = formation (回到站位)
17 = clear_to_center (清球到中路)
需要改进的函数（按优先级）：

---
① find_best_pass_target — 传球目标选择（最重要）
当前逻辑（player/decision.py:379）：
- 候选者必须比自己更靠近对方球门（distance_to_their_goal 更小）
- 传球路线无对手（pass_lane_clear，3.0米缓冲）
- 接球后到球门路线无对手（receiver_goal_lane_clear，3.0米缓冲）
- 最终选 x 坐标最大、y 绝对值最小的候选者
问题：
- 缓冲 3.0 米太严，导致传球机会很少，经常放弃传球去运球
- 没考虑接球队员周围有多少对手（危险程度）
- 没考虑传球成功率（距离过远 pass 会失误）
改进建议（你来写）：
def find_best_pass_target(wm):
    # 建议增加的逻辑：
    # 1. pass_lane_clear 的缓冲从 3.0 改为 2.0，提高传球频率
    # 2. 增加"接球队员周围最近对手距离"评分（越远越好）
    # 3. 增加距离惩罚（超过 25 米传球失误率高）
    # 4. 候选者评分 = f(x坐标, 接球区安全度, 传球距离)
    pass

---
② in_shooting_range — 射门判断（player/decision.py:370）
当前逻辑：距离 < 20 且角度 < 30度
问题：
- 忽略了守门员位置（守门员正对着你时不应射）
- 角度是相对自身朝向，但没用球门宽度换算
改进建议：
def in_shooting_range(wm):
    # 建议：
    # 1. 距离 < 15 米时无条件可射（近射把握大）
    # 2. 15-20 米时检查角度 < 25 度
    # 3. 可选：检查是否有对手在射门路线上（point_to_segment_distance）
    pass

---
③ decide_goalkeeper_action — 守门员策略（player/decision.py:293）
当前逻辑：
- 有球就清球（向最靠前的队友）
- 球在禁区就冲上去
- 否则跟踪球的 y 轴，站在 x = -51 处
问题：
- 清球目标只选 x 最大的队友，可能传给被包夹的人
- 没有处理对手单刀球的情况（对手一对一冲门）
改进建议：
def goalkeeper_clear_action(wm):
    # 建议：清球优先选择"最靠前且传球路线清晰"的队友
    # 而不只是 x 坐标最大的队友
    pass

def decide_goalkeeper_action(wm):
    # 增加：如果对手在禁区内比我们更快到球（self_min 比 opp_min 大很多），
    # 直接冲上去铲球/站位
    pass

---
2. train/action.py — 动作到执行的映射
这个文件将 RL 模型选择的 action_id 转化为实际执行动作。
可以改进的点：
dribble_left/right 的偏移量（player/decision.py:93-106）：
# 当前：lateral_offset = ±12.0（向左/右跑 12 米）
# 问题：12 米太大，容易跑出边界或被包夹
# 建议改为 ±6.0，更保守的路线

---
开发流程
1. 你在本地写好代码（不需要跑仿真）
2. 发给我（A），我部署到 Ubuntu 服务器跑测试
3. C 分析比赛数据，告诉我们效果
4. 根据反馈调整
本地测试方法（不需要仿真）：
# 可以写单元测试，mock 掉 wm（WorldModel）对象
# 例如测试 find_best_pass_target 的逻辑

---
给 C 的说明（数据分析 & 可视化）
日志文件在哪
服务器路径：/home/ccczorange/robocup/logs/
我会把这个目录打包发给你。
目录结构：
logs/
├── train/                          # RL 训练日志（每次训练一个子目录）
│   ├── episode_000001_xxx/         # 第1回合
│   │   ├── rcssserver.log          # 服务器日志（含比分）
│   │   ├── home/player_10.log      # 10号球员日志（RL控制的球员）
│   │   └── home_launcher.log
│   ├── episode_000002_xxx/
│   ...
│   ├── worker0_episode_000001_xxx/ # 多进程训练时 worker0 的日志
│   ├── worker1_episode_000001_xxx/
│   ...
├── rcssserver.log                  # 全局服务器日志
└── player_10.log                   # 球员日志
比赛回放文件（.rcl 格式）：
/home/ccczorange/robocup/
├── 20260318145845-RLTeam_0-vs-RuleOpponent_0.rcl   # 文件名含比分
├── 20260318145845-RLTeam_0-vs-RuleOpponent_0.rcg   # 二进制回放（不需要）
...
文件名格式：时间戳-左队_左队分-vs-右队_右队分.rcl

---
任务 1：解析 rcssserver.log 提取比分
每个 episode 目录下有 rcssserver.log，里面包含：
Kick_off_left
Goal_Left (cycle=xxx)    # 左队进球
Goal_Right (cycle=xxx)   # 右队进球
Time_Over               # 比赛结束
你需要写 analysis/parse_logs.py：
# 遍历 logs/train/ 下的所有 episode 目录
# 从每个 rcssserver.log 提取：
#   - episode 编号（从目录名 episode_NNNNNN_xxx 提取）
#   - 最终比分（左队得分、右队得分）
#   - 哪支队是 RLTeam（从文件名或日志判断）
# 输出：episode_id, rl_score, opp_score, goal_diff

---
任务 2：解析 .rcl 文件提取动作统计
.rcl 是纯文本格式，每行是一个周期的事件记录。格式示例：
(cycle 100)(ball_pos -10.5 3.2)(player RLTeam 10 -8.3 4.1 0.5 0.2)(kick ...)
你需要写 analysis/parse_rcl.py：
# 目标：统计 10号球员（RL控制）每种动作被选择了多少次
# 动作名称在 player_10.log 里有记录（label 字段）：
#   shoot / dribble / pass_N / chase_ball / move_to_formation / ...
# 
# 解析方法：
# 1. 读取对应 episode 的 home/player_10.log
# 2. 搜索包含 action_label 的行（如 "rl_dribble_forward", "rl_shoot"）
# 3. 统计各类动作出现次数
player_10.log 里每个动作执行时会有类似：
[debug] label=rl_shoot cycle=234
[debug] label=rl_dribble_forward cycle=235

---
任务 3：绘制训练曲线
你需要写 analysis/plot_rewards.py：
import matplotlib.pyplot as plt
import pandas as pd

# 数据来源：parse_logs.py 的输出
# 横轴：episode 编号
# 图1：进球差（rl_score - opp_score）随 episode 变化
# 图2：RL 队进球数随 episode 变化（判断是否在学习进攻）
# 图3：各动作使用频率随 episode 变化（判断策略是否收敛）

# 建议用滑动平均（window=10）平滑曲线

---
任务 4：对比图
不同阶段策略效果对比（例如 B 改了规则策略前后）：
# 对比两个时间段的比赛数据
# 例如：20260314 之前 vs 20260318 之后
# 指标：胜率、平均进球数、平均失球数

---
环境要求（非常简单）
pip install matplotlib pandas numpy
不需要安装任何仿真相关的东西。

---
A（你）与 B、C 的协作接口
你发给 B 的：
- 完整的 player/decision.py 和 train/action.py 当前版本
- B 改好后发给你 → 你替换文件 → 跑测试 → 把结果发给 C
你发给 C 的：
- logs/train/ 目录（打包）
- 所有 .rcl 文件（在 /home/ccczorange/robocup/ 根目录）
- 告诉 C：RLTeam 是我们队，RuleOpponent 是对手
你从 C 拿到的：
- 进球率曲线图
- 动作分布图
- 用来判断：奖励设计是否有效、某类动作是否被过度/过少选择

---
关键约束（B 和 C 都要知道）
暂时无法在飞书文档外展示此内容
